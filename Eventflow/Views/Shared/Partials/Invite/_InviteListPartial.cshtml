@using Eventflow.ViewModels.Invite.Page
@using Eventflow.ViewModels.Shared
@model InvitePageViewModel

@if (Model.Invites?.Any() == true)
{
    foreach (var invite in Model.Invites)
    {
        if (invite != null)
        {
            @await Html.PartialAsync("~/Views/Shared/Partials/Invite/_InviteCardPartial.cshtml", invite)
        }
    }

    @await Html.PartialAsync("~/Views/Shared/Partials/Reminder/_PaginationPartial.cshtml", new PaginationViewModel
    {
        CurrentPage = Model.CurrentPage,
        TotalPages = Model.TotalPages,
        SearchTerm = Model.SearchTerm,
        SortBy = Model.SortBy,
        State = ((int)Model.CurrentStatus).ToString()
    })
}
else
{
    @await Html.PartialAsync("~/Views/Shared/Partials/Invite/_InviteEmptyStatePartial.cshtml")
}
